{#
/**
 * @file default_entity.html.twig
 * Default theme implementation to present Default entity data.
 *
 * This template is used when viewing Default entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_default_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('default_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
